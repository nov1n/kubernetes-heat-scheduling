apiVersion: v1
kind: Pod
metadata:
    name: "init"
    namespace: "heat-scheduling"
    labels:
        sel: "init"
spec:
    containers:
        - name: "init"
          image: "gcr.io/bep-matosi/init:1.0.0"
          ports:
            - containerPort: 8090
    restartPolicy: "Always"

